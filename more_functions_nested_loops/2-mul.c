#include "main.h"
#include <stdio.h>

/**
* mul - function that multiplies two integers and return the result.
* @a: first number input
* @b: second number input
* Return: a * b
*/

int mul(int a, int b)
{
return (a * b);
}
